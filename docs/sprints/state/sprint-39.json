{
  "id": 39,
  "title": "Give a blocked sprint a way back that keeps its gates, let headless LiveQA run node, and stop a named failing test standing in for a decision",
  "phase": "dev_build",
  "qa1_audit_result": "CONDITIONAL",
  "qa1_audit_file_hash": null,
  "qa1_audited_tree_hash": null,
  "last_shipped_commit": null,
  "groundtruth_result": null,
  "live_loop_audit_trees": [],
  "audit_rounds": 1,
  "live_test_rounds": 0,
  "started": "2026-09-18T05:01:58+00:00",
  "completed": null,
  "history": [
    {
      "ts": "2026-09-18T05:01:58+00:00",
      "actor": "system",
      "event": "sprint_started",
      "detail": ""
    },
    {
      "ts": "2026-09-21T17:15:34+00:00",
      "actor": "qa1",
      "event": "audit",
      "detail": "CONDITIONAL: QA1 gate-1 audit, sprint 39, round 1. Audited commit dd690a6 (on bdbce89, the sprint start). The sprint file was read fresh at HEAD and is unchanged since bdbce89. Verdict: CONDITIONAL. One demonstrated defect, MEDIUM, with a small fix. Everything else in Reqs 1-6 is met.\n\nSCOPE NOTE. HEAD is 4792f9d, two commits past dd690a6, but both (5ec8588, 4792f9d) touch only sprint files and the registry, which SHIP_HASH_EXCLUDE_PATTERNS excludes from the audited tree hash. No sprint 40 or 41 code is in what this audit covers.\n\nREQUIRED FIX.\n1. [MEDIUM] Blocking a sprint that is already blocked records pre_block_phase=\"blocked\", and the next re-file then \"restores\" the sprint to phase blocked while the registry says in_progress and the file sits in 2-in-progress/.\n - Cause: cmd_block records state[\"phase\"] as pre_block_phase with no check that the phase is a real working phase, and cmd_start's keep-gates branch restores whatever it finds.\n - Reproduced in a scratch install built from dd690a6's own scripts:\n   - sprint 1: start, commit, /sprint-qa1 PASS, /sprint-block (\"decision pending\") gives phase blocked, pre_block qa1_audit. Correct.\n   - /sprint-block again (\"still pending\") is accepted and gives phase blocked, pre_block blocked.\n   - /sprint-start then prints \"Gates KEPT: ... returns directly to its pre-block phase, 'blocked'\" and leaves phase blocked, pre_block None, registry status in_progress, file at docs/sprints/2-in-progress/sprint-1_double-block-sprint.md.\n - Why it matters: the sprint is now in a state no reader can act on. /sprint-status says blocked while the registry and folder say in-progress, and the printed line tells the reader to pick up where it left off in a phase that means \"stopped\". A second /sprint-start then takes the reset path (pre_block_phase is now None) and clears the QA1 PASS, so a double block silently costs exactly the gate preservation this sprint adds.\n - The underlying \"block an already-blocked sprint\" allowance predates this sprint, but it was harmless until Req 2a started storing that phase and Req 2b started restoring it. It is this change's defect.\n - Suggested fix, either is fine: have cmd_block refuse when state[\"phase\"] == \"blocked\" (nothing to do, nothing changed, consistent with its complete/aborted guard), or have it not overwrite pre_block_phase when the sprint is already blocked, so the original pre-block phase survives a re-block. A guard in cmd_start against restoring a non-working phase would be belt and braces.\n - Test to add: block twice, then re-file, and assert the sprint does not come back in phase blocked.\n\nWHAT IS FINE, verified rather than read.\n - Req 1: LIFECYCLE_OWNED_FRONTMATTER_PATTERNS holds exactly one entry, the status: line, with the reason stated, and reuses the same regex shape update_frontmatter_status writes. title:/original_title: are not excluded, and cmd_rename's docstring was updated to say so. Every reader and writer of qa1_audit_file_hash goes through the new functions: cmd_qa1 writes audited_file_hash plus the scheme, cmd_dev_done, cmd_liveqa and cmd_start all replay via compute_audit_hash, and cmd_override re-stamps with the new scheme (1b).\n - Req 1a: verified directly. With a planted legacy whole-file hash and no qa1_audit_hash_scheme key, an unchanged file passes /sprint-dev-done, and after a real body edit it refuses with the drift message. So an old hash can neither fail a genuinely unchanged file nor pass an edited one.\n - Also verified: a legacy-scheme sprint that goes through block then start keeps its gates, because the status: line round-trips back to the audited value, so even the whole-file hash matches again. Correct outcome, worth knowing it is incidental rather than designed.\n - Req 2a: cmd_block records pre_block_phase before overwriting phase, and also names it in the history event.\n - Req 2b: verified end to end. A sprint taken to liveqa_live (start, commit, PASS, dev-done, ship), blocked, then re-filed with the file untouched returns to liveqa_live with qa1_audit_result PASS, last_shipped_commit and rounds intact, and prints the KEPT line. pre_block_phase is cleared afterward.\n - Req 2c: verified. With a real body edit made while blocked, the re-file prints \"Gates RESET: the sprint file has changed since QA1's PASS\" and leaves phase dev_build with every gate field cleared. The reset branch also clears the new qa1_audit_hash_scheme and pre_block_phase.\n - Req 2d: verified. A commit made while the sprint was blocked, after a gate-keeping re-file to liveqa_live, is still refused by /sprint-reship on the tree check (\"no QA1 PASS currently on record for it\"). No ship, reship or liveqa check was loosened; the only lifecycle-owned exclusion added is to the SPRINT-FILE hash, never the tree hash.\n - Req 2e: CLAUDE.md, sprint-start.md and sprint-block.md all describe both paths, and the stale \"current sprint's own Out of Scope\" pointer is gone (0 occurrences).\n - Req 3: the findings record exists in docs/sprint-12-permission-scope-findings.md with the CLI version anchor (claude 2.1.276), the method, a per-probe table for (i), (ii) and (iii) plus a bonus probe, grades (three CONFIRMED, one WEAK with the reason), two runs each, and a discarded round 1 explained. The conclusion follows Req 3b: node * does not widen reach because npx * already permits arbitrary program-mediated writes inside and outside the working directory, measured fresh rather than carried over from sprint 19, so no escalation was needed. The same reasoning is repeated at the grant itself in run-role.js, and liveqa.md states what the grant permits and that the no-writing-source rule is an instruction, not a tool restriction. The missing raw transcripts are named honestly in the record.\n - Req 4: the citation rule is in qa1.md, dev-team-1.md and dev-team-2.md, worded generally, with the \"first report routes it to Master Controller as unowned, later reports say still unowned\" rule and one sentence of FMC history as the reason.\n - Req 5: package.json is 0.2.14 against published 0.2.13 (npm view), with a CHANGELOG entry covering Reqs 1-4.\n - Req 6: QA1 ran both suites at dd690a6 in a clean clone. node scripts/launcher_test.js: ALL LAUNCHER TESTS PASSED, exit 0. bash scripts/smoke_test.sh: ALL SMOKE TESTS PASSED, exit 0, with nine sprint-39 sections. pyflakes is clean.\n\nADVISORY, not blocking. The Req 3 measurements are anchored at claude 2.1.276; this machine is now on 2.1.278. That is what warnIfPermissionFindingsStale exists to surface, and LiveQA's own criterion re-confirms the denials at gate time on whatever version is current then. Record that version.\n\nFOR LIVEQA. Beyond the sprint's own criteria: after the gate-preserving re-file check, also block a sprint twice before re-filing and confirm it does not return in phase blocked (item 1, once fixed)."
    }
  ],
  "last_claim": {
    "claude_session_id": "6f40f6d7-7669-5fd2-a92b-6d69e72ba681",
    "claude_agent": "qa1",
    "ts": "2026-09-21T17:15:34+00:00"
  },
  "qa1_audit_hash_scheme": null
}
